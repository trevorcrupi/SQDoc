<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{ globals.projectName }}</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <h1>{{ globals.projectName }}</h1>
  <h3>{{ globals.description }}</h3>

  {{{body}}}

</body>
</html>
